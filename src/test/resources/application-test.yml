spring:
  application:
    name: hhplus-week2


  datasource:
    url: jdbc:h2:mem:hhplus-lecture
    driver-class-name: org.h2.Driver
    username: root
    password: 1111
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true